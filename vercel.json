{
  "framework": "vite",
  "buildCommand": "npx vite build && npx esbuild server.ts --bundle --platform=node --format=esm --packages=external --outfile=backend/runtime.mjs",
  "outputDirectory": "dist",
  "functions": { "api/portal.ts": { "maxDuration": 60 } },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/portal" },
    { "source": "/health", "destination": "/api/portal" }
  ]
}
